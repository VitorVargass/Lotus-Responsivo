function redirecionar(lado) {
    if (lado === 'esquerda') {
        window.location.href = 'esquerda.html';
    } else if (lado === 'direita') {
        window.location.href = 'Direita.html';
    }
}
